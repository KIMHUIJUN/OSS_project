<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @album.title %>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Recommend</th>
    </tr>
  </thead>

  <tbody>
    <% @tracks.each do |track| %>
      <tr>
        <td><%= link_to track.title, track_path(track.id) %></td>
        <td><%= track.recommend %></td>
        </tr>
    <% end %>
  </tbody>
</table>
</p>

<p>
  <strong>Artist:</strong>
  <%=link_to @album.artist.real_name, artist_path(@album.artist.id)%>
</p>

<p>
  <strong>release_com:</strong>
  <%= @album.release_com %>
</p>
<p>
  <strong>release_com:</strong>
  <%= @album.recommend%>
</p>
<p>
  <strong>album.id:</strong>
  <%= @album.id%>
</p>

<img src= <%=@album.album_link%>/>
<br>
<% if user_signed_in? %>
	
<%= render 'commends/form', commend: @commend %>
<%end%>
<h4>
	댓글 <%= @commends.size %> 개
</h4>
<table>
  <thead>
    <tr>
	  <th>작성자</th>
  	  <th>Commend</th>
  
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @commends.each do |commend| %>
	  <tr>
		  <td><%= commend.user.name %></td>

		  <td><%= commend.commend %></td>

	  </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Back', albums_path %>

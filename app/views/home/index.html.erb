<p id="notice"><%= notice %></p>

<body>
	<div style="margin-top: 200px; margin-left: 200px">
<% if user_signed_in? %>
  <%= link_to'로그아웃', destroy_user_session_path(@current_user), method: :delete, data:{ confirm: "Are you sure?" } %>
<% else %>
  <%= link_to('로그인', new_user_session_path, :class => 'item borderless') %>
<% end %>
</div>
</body>